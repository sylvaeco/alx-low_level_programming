#include <stdio.h>
#include "main.h"

/**
 * main - check the code
 * @c: This takes an integer value
 * Return: Always for success..
 */
int main(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
