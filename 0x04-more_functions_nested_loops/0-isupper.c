#include "main.c"
#include <stdio.h>

/**
 * main - checks the code for uppercase
 *
 * Return: Always 0 for success
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return ('1');
	}
	else
	{
		return ('0');
	}
	return (0);
}
